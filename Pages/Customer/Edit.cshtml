@page
@model DC2.UI.Pages.Customer.EditModel
@{
    if (User.Identity.IsAuthenticated)
    {
        Layout = "_LayoutLogedIn";
    }
}

<center>
    <h1>Edit Current Customer</h1>
    <br>
</center>

@*<div>@Model.CurrentCustomer.FirstName</div>
    <form asp-page-handler="Save">
    <input type="hidden" asp-for="CurrentCustomer.Id" value="@Model.CurrentCustomer.Id" />
    <input type="text" asp-for="CurrentCustomer.FirstName" value="@Model.CurrentCustomer.FirstName" />
    <input type="text" asp-for="CurrentCustomer.LastName" value="@Model.CurrentCustomer.LastName" />
    <input type="email" asp-for="CurrentCustomer.Email" value="@Model.CurrentCustomer.Email" />
    <input type="text" asp-for="CurrentCustomer.PhoneNumber" value="@Model.CurrentCustomer.PhoneNumber" />
    <input type="text" asp-for="CurrentCustomer.Secret" value="@Model.CurrentCustomer.Secret" />

    <button type="submit" class="btn btn-primary">Submit</button>

    </form>*@

<form asp-page-handler="Save">
    <input type="hidden" class="form-control" asp-for="CurrentCustomer.Id" value="@Model.CurrentCustomer.Id">
    <div class="form-group">
        <label for="firstname">First Name</label>
        <input type="text" class="form-control" asp-for="CurrentCustomer.FirstName" value="@Model.CurrentCustomer.FirstName" placeholder="Enter First Name">
    </div>
    <div class="form-group">
        <label for="lastname">Last Name</label>
        <input type="text" class="form-control" asp-for="CurrentCustomer.LastName" value="@Model.CurrentCustomer.LastName" placeholder="Enter Last Name">
    </div>
    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" class="form-control" asp-for="CurrentCustomer.Email" value="@Model.CurrentCustomer.Email" id="email" placeholder="Email">
    </div>
    <div class="form-group">
        <label for="phonenumber">Phone Number</label>
        <input type="text" class="form-control" asp-for="CurrentCustomer.PhoneNumber" value="@Model.CurrentCustomer.PhoneNumber" id="email" placeholder="Phone Number">
    </div>
    <div class="form-group">
        <label for="secret">Secret</label>
        <input type="text" class="form-control" asp-for="CurrentCustomer.Secret" value="@Model.CurrentCustomer.Secret" id="secret" placeholder="Secret">
    </div>
    @*<div class="form-check">
        <input type="checkbox" class="form-check-input" id="exampleCheck1">
        <label class="form-check-label" for="exampleCheck1">I accept all cookies on this website</label>
        </div>*@
    <button type="submit" class="btn btn-primary">Submit</button>
    <a asp-page="./Customer" class="btn btn-primary">Back</a>
</form>
@*<h1>TEST</h1>
    <h6>ID: @Model.CurrentCustomer.Id</h6>
    <h6>Namn: @Model.CurrentCustomer.FirstName @Model.CurrentCustomer.LastName</h6>
    <h6>Email: @Model.CurrentCustomer.Email</h6>
    <h6>PhoneNumber: @Model.CurrentCustomer.PhoneNumber</h6>
    <h6>Secret: @Model.CurrentCustomer.Secret</h6>*@